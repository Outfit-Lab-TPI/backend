# ===========================================

spring.application.name=outfitlab-backend

#spring.datasource.url=${SPRING_DATASOURCE_LOCAL_URL}
#spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
#spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
#spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# == julian -> tripo.api.key=tsk_IDtE_QEJ0ZCwQJDvy7HEotADi6auStMdj5dka5Z3LpV
tripo.api.key=tsk_IBWdYCmaC-WBnLQBGu0BPe2fNbGoWt2gdB4I8Lrh0Cn

# ===== Development Configuration =====
# ===== MinIO Configuration (Local Development) =====
minio.endpoint=http://localhost:9000
minio.access-key=minioadmin
minio.secret-key=minioadmin123
minio.bucket.images=outfitlab-images
minio.bucket.models=outfitlab-models

# ===== Security (basic for development) =====
spring.security.user.name=admin
spring.security.user.password=admin123
spring.main.allow-bean-definition-overriding=true

# ===== Server Configuration =====
server.port=8080

# ===== Logging Configuration =====
logging.level.root=INFO
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.com.outfitlab.project=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# ===== File Upload Configuration =====
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# ===== Management/Actuator =====
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

#spring.datasource.url=jdbc:h2:mem:testdb
#spring.datasource.driver-class-name=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=

# ====== CONFIG PARA DB en RENDER =====
spring.datasource.url=${SPRING_DATASOURCE_RENDER_URL}
spring.datasource.username=${SPRING_DATASOURCE_RENDER_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_RENDER_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# ===== Frontend / CORS =====
app.frontend.base-url=${FRONTEND_BASE_URL:http://localhost:5173}
app.cors.allowed-origins=${FRONTEND_ORIGINS:http://localhost:5173,https://*.vercel.app}

# ===== mercado pago =====
mercadopago.access.token=${MP_ACCESS_TOKEN}
mercadopago.urls.back=${app.frontend.base-url}/suscripcion-finalizada

# ====== CONFIG PARA Gmail =====
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=tpi8bits@gmail.com
spring.mail.password=wndj gngh imwn bnzt
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

app.frontend.login-url=http://localhost:5173/login

gemini.api.key=${GEMINI_API_KEY}
